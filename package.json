{
  "name": "testing-workshop-playwright",
  "packageManager": "yarn@4.6.0",
  "devDependencies": {
    "@playwright/test": "^1.50.1",
    "@types/node": "^22.13.4",
    "playwright": "^1.50.1",
    "typescript": "^5.7.3"
  },
  "scripts": {
    "install playwright": "yarn playwright install",
    "test chrome": "playwright test --project chromium --headed",
    "test": "yarn playwright test",
    "test:ui": "yarn playwright test --ui"
  }
}
